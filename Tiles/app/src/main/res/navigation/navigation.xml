<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/home2">

    <fragment
        android:id="@+id/home2"
        android:name="com.example.tiles.home"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_home2_to_playable"
            app:destination="@id/playable" />
    </fragment>
    <fragment
        android:id="@+id/playable"
        android:name="com.example.tiles.playable"
        android:label="fragment_playable"
        tools:layout="@layout/fragment_playable" />
</navigation>