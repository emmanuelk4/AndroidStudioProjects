//Urban Bliss merch// Referral links// Promos
import 'package:flutter/material.dart';
import 'package:urbanbliss/blocNav/blocNavigation.dart';

class Shop extends StatelessWidget with NavigationStates {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
          "Shop shiiit"
      ),
    );
  }
}
